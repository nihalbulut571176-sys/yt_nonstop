# Final Review Director

Goal: approve the whole package as a viewer-retention system.

- Check first 30–60 seconds strength.
- Check prompt coverage, variety, and slideshow risk.
- Check export readiness before generation.
- Mark package as `ready_for_generation`, `ready_with_warnings`, or `requires_rework`.
