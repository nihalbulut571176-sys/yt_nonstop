# Scene Planner

Goal: convert `cleaned.srt` into canonical `scene_plan.json`.

- Preserve timing.
- Group subtitle blocks into production scenes.
- Assign stable `scene_id`.
- Include `voice_text`, duration, and placeholder creative fields.
